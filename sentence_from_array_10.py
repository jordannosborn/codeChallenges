# Challenge: Sentence from array
# Create a function called sentence_from_array that accepts an array of strings
# an combines each string into a single sentence separated by spaces.
# Note that for this challenge it's okay that the string ends in a space.


def sentence_from_array(the_array):
  return ""


assert sentence_from_array(["What", "the", "fuck"]) == "What the fuck "
assert sentence_from_array(["Hubert", "Farnsworth"]) == "Hubert Farnsworth "